/* eslint-disable */
/**
 * name: 创建编辑框，可拖动
 * author: chase
 * time: 2018/10/27
 */
export default {
    bind(el, binding, vnode, oldVnode) {
        const parentDiv = el;
        let ifScroll = parentDiv.offsetHeight;
        // console.log(ifScroll);
        // console.log(parentDiv.scrollHeight);

    }
}